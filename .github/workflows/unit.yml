# ⚠️ DEPRECATED: This workflow is replaced by ci-cd.yml
# Please use the unified CI/CD pipeline instead
# This file is kept for reference only

name: Test & Build Master (DEPRECATED)

on:
  # Disabled - use ci-cd.yml instead
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm run test

      - name: Build production
        run: npm run build
